<UserControl x:Class="StockPoint.WPF.Views.ConsultaOrdenUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:StockPoint.WPF.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1100">

    <UserControl.DataContext>
        <vm:ConsultaOrdenViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="18,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.82"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LightInput" TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1" CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#2563EB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F1F5F9">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Cabecera -->
        <Border Grid.Row="0" Background="White"
                BorderBrush="#E5E7EB" BorderThickness="0,0,0,1" Padding="18,11">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Consultar Orden" Foreground="#111827" FontSize="15" FontWeight="SemiBold"/>
                <TextBlock Text="Búsqueda por número de orden" Foreground="#9CA3AF" FontSize="11" Margin="0,1,0,0"/>
            </StackPanel>
        </Border>

        <!-- Buscador -->
        <Border Grid.Row="1" Background="White" BorderBrush="#E5E7EB" BorderThickness="1"
                CornerRadius="4" Margin="16,14,16,0" Padding="18,16">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <StackPanel Width="220" Margin="0,0,10,0">
                        <TextBlock Text="NÚMERO DE ORDEN" Foreground="#6B7280" FontSize="10" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBox Style="{StaticResource LightInput}"
                                 Text="{Binding NumeroBusqueda, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <Button Content="Buscar" Style="{StaticResource ActionButton}" Background="#2563EB"
                            VerticalAlignment="Bottom" Command="{Binding BuscarCommand}"/>
                </StackPanel>

                <TextBlock Text="{Binding Mensaje}" Foreground="#B91C1C" FontSize="12" Margin="0,10,0,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Mensaje}" Value="">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- Resultado -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="16,14,16,16"
                      Visibility="{Binding TieneOrden, Converter={StaticResource BoolToVis}}">
            <StackPanel>

                <!-- Datos de la cabecera -->
                <Border Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="4" Padding="20,16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="ORDEN N.°" Foreground="#9CA3AF" FontSize="10" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Orden.NumeroOrden}" Foreground="#111827" FontSize="20" FontWeight="Bold" Margin="0,2,0,0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="CLIENTE" Foreground="#9CA3AF" FontSize="10" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Orden.NombreCliente}" Foreground="#111827" FontSize="14" Margin="0,4,0,0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="FECHA" Foreground="#9CA3AF" FontSize="10" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Orden.FechaOrden, StringFormat=dd/MM/yyyy HH:mm}" Foreground="#111827" FontSize="14" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Detalle de productos -->
                <TextBlock Text="PRODUCTOS DE LA ORDEN" Foreground="#6B7280" FontSize="10" FontWeight="Bold" Margin="2,18,0,8"/>
                <Border BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="4">
                    <DataGrid ItemsSource="{Binding Orden.Detalles}"
                              Background="White" RowBackground="White" AlternatingRowBackground="#F9FAFB"
                              Foreground="#374151" BorderThickness="0"
                              GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#E5E7EB"
                              AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                              HeadersVisibility="Column" RowHeight="34">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F9FAFB"/>
                                <Setter Property="Foreground" Value="#6B7280"/>
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="FontSize" Value="10"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="BorderBrush" Value="#E5E7EB"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="CÓDIGO"       Binding="{Binding CodigoProducto}"                            Width="120"/>
                            <DataGridTextColumn Header="PRODUCTO"     Binding="{Binding NombreProducto}"                           Width="*"/>
                            <DataGridTextColumn Header="PRECIO UNIT." Binding="{Binding PrecioUnitario, StringFormat='₡{0:N0}'}"   Width="110"/>
                            <DataGridTextColumn Header="CANT."        Binding="{Binding Cantidad}"                                 Width="60"/>
                            <DataGridTextColumn Header="IVA"          Binding="{Binding Impuesto, StringFormat='₡{0:N0}'}"         Width="90"/>
                            <DataGridTextColumn Header="SUBTOTAL"     Binding="{Binding PrecioSubtotal, StringFormat='₡{0:N0}'}"   Width="110"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <!-- Totales -->
                <Border Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="4"
                        Padding="20,14" Margin="0,12,0,0" HorizontalAlignment="Right" Width="280">
                    <StackPanel>
                        <Grid Margin="0,3">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBlock Text="Subtotal" Foreground="#6B7280" FontSize="13"/>
                            <TextBlock Grid.Column="1" Text="{Binding Orden.Subtotal, StringFormat='₡{0:N0}'}" Foreground="#374151" FontSize="13"/>
                        </Grid>
                        <Grid Margin="0,3">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBlock Text="IVA" Foreground="#6B7280" FontSize="13"/>
                            <TextBlock Grid.Column="1" Text="{Binding Orden.Impuesto, StringFormat='₡{0:N0}'}" Foreground="#374151" FontSize="13"/>
                        </Grid>
                        <Rectangle Height="1" Fill="#E5E7EB" Margin="0,8"/>
                        <Grid>
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBlock Text="Total" Foreground="#111827" FontSize="16" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Orden.TotalOrden, StringFormat='₡{0:N0}'}" Foreground="#111827" FontSize="16" FontWeight="Bold"/>
                        </Grid>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
